#![forbid(unsafe_code)]

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    // download migration sql files and check the most recent commit
    // replace migration sql files if there is a new commit

    Ok(())
}
